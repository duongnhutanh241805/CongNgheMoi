<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Edit Product</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .product-image {
            max-width: 200px;
            max-height: 200px;
            object-fit: cover;
            margin: 10px 0;
        }
    </style>
</head>

<body>
    <h2>Edit Product</h2>

    <form action="/edit/<%= product.id %>" method="post" enctype="multipart/form-data">
        Name: <input name="name" value="<%= product.name %>" required><br><br>
        Price: <input name="price" type="number" step="0.01" value="<%= product.price %>" required><br><br>
        Quantity: <input name="quantity" type="number" value="<%= product.quantity %>" required><br><br>
        
        <% if (product.imageUrl) { %>
            <div>
                Current Image:<br>
                <img src="<%= product.imageUrl %>" alt="<%= product.name %>" class="product-image">
            </div>
        <% } %>
        
        New Image (optional): <input name="image" type="file" accept="image/*"><br><br>
        <button>Update</button>
    </form>

    <br>
    <a href="/">⬅ Back</a>
</body>

</html>
